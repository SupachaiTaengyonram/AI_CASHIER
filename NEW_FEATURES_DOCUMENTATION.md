# 📝 ฟีเจอร์ใหม่ - AI Cashier Enhancement v2.0

เอกสารนี้อธิบายฟีเจอร์ใหม่ที่เพิ่มเข้าไปในระบบ AI Cashier

## 🎯 ฟีเจอร์ที่เพิ่มเข้ามา

### 1️⃣ ปุ่มเรียกพนักงาน (Call Staff Button)
**สำหรับ:** ลูกค้าหน้าร้าน  
**ตำแหน่ง:** หน้า Home (`home.html`) - ปุ่มสีเหลืองพร้อมไอคอน 🔔

#### ฟังก์ชันการทำงาน:
- ลูกค้าสามารถแตะปุ่มเรียกพนักงาน
- ระบบจะบันทึกการเรียก พร้อมเวลาและเหตุผล
- ปุ่มจะแสดงสถานะ "กำลังเรียก..." จากนั้นเปลี่ยนเป็น "ส่งสัญญาณแล้ว" เมื่อสำเร็จ

#### API Endpoint:
```
POST /api/staff/call/
Headers: X-CSRFToken, Content-Type: application/json
Body: {
  "reason": "ลูกค้าขอความช่วยเหลือ" (optional)
}
```

#### ไฟล์ที่เกี่ยวข้อง:
- `api_views.py` - `call_staff_api()`
- `services.py` - `StaffCallService`
- `home.html` - ปุ่มและจาวาสคริปต์

---

### 2️⃣ ปุ่มยกเลิกออเดอร์ (Cancel Order Button)
**สำหรับ:** ลูกค้าออนไลน์ และพนักงาน  
**ตำแหน่ง:** หน้า Order Management (`order_management.html`) - ปุ่มสีแดง ✕

#### ฟังก์ชันการทำงาน:
- แสดงเฉพาะสำหรับออเดอร์ออนไลน์ (online)
- เมื่อยกเลิก ออเดอร์จะหายไปจากรายการทั่นที
- มีการยืนยันก่อนยกเลิก

#### API Endpoint:
```
POST /api/orders/cancel/
Headers: X-CSRFToken, Content-Type: application/json
Body: {
  "order_id": <id>,
  "reason": "ยกเลิกจากหน้าจัดการออเดอร์" (optional)
}
```

#### ไฟล์ที่เกี่ยวข้อง:
- `api_views.py` - `cancel_order_api()`
- `services.py` - `OrderCancellationService`
- `order_management.html` - ปุ่มและจาวาสคริปต์

---

### 3️⃣ ระบบแจ้งเตือน Email สินค้าใกล้หมด
**สำหรับ:** เจ้าของระบบ (Superuser)  
**การตั้งค่า:** Low stock threshold = 10 ชิ้น (สามารถแก้ไขได้)

#### ฟังก์ชันการทำงาน:
- ตรวจสอบสินค้าที่มีจำนวนน้อยกว่า 10 ชิ้น
- ส่ง Email ไปยัง Admin เมื่อมีสินค้าใกล้หมด
- สามารถรันด้วยคำสั่ง Management Command หรือ API

#### การใช้งาน:

**วิธีที่ 1: Management Command (ใช้ Cron Job)**
```bash
python manage.py check_low_stock
```

**วิธีที่ 2: API (สำหรับ Admin)**
```
GET /api/inventory/check-low-stock/
```

#### Email Template:
```
เรื่อง: ⚠️ แจ้งเตือน: สินค้าใกล้หมด

เนื้อหา:
สวัสดีครับ,

มีสินค้าที่ตัวขต่อไปนี้ใกล้หมดแล้ว (น้อยกว่า 10 ชิ้น):

• สินค้า A - เหลือ 5 ชิ้น
• สินค้า B - เหลือ 8 ชิ้น

โปรดเติมสต็อกโดยเร็วที่สุด

AI Cashier System
```

#### ไฟล์ที่เกี่ยวข้อง:
- `services.py` - `InventoryService`
- `api_views.py` - `check_low_stock_api()`
- `management/commands/check_low_stock.py` - Management command

---

### 4️⃣ ข้อมูลวิเคราะห์ใหม่ในหน้า Overviews (Admin Only)

#### A. ค่าเฉลี่ยต่อบิล (Average Order Value - AOV)
- แสดงว่าลูกค้าใช้เงินเฉลี่ยเท่าไหร่ต่อออเดอร์
- คำนวณจาก 30 วันที่ผ่านมา
- ช่วยวัดมูลค่าลูกค้า

#### B. อัตราการยกเลิก (Cancellation Rate)
- แสดงเปอร์เซ็นต์ของออเดอร์ที่ยกเลิก
- คำนวณจาก 30 วันที่ผ่านมา
- ช่วยติดตามปัญหาลูกค้า

#### C. คำค้นหาหลักของลูกค้า (Top User Queries)
- แสดง 5 อันดับแรกของคำถามที่ลูกค้าถาม
- ดึงข้อมูลจากประวัติแชท
- แสดงความถี่และเปอร์เซ็นต์

#### ตำแหน่งแสดงผล:
หน้า Overviews → หลังจากชาร์ต Best Seller และ Weekly Chart

#### API Endpoints:
```
GET /api/analytics/aov/?days=30
GET /api/analytics/cancellation-rate/?days=30
GET /api/analytics/top-queries/?limit=5
```

#### ไฟล์ที่เกี่ยวข้อง:
- `services.py` - `OrderAnalyticsService`
- `api_views.py` - Analytics APIs
- `views.py` - `OverviewsView` (get_context_data)
- `overviews.html` - UI Display

---

## 📂 โครงสร้างไฟล์ใหม่

```
aicashier/
├── services.py                          (ใหม่) - Business logic
├── api_views.py                         (ใหม่) - API endpoints
├── management/
│   └── commands/
│       └── check_low_stock.py           (ใหม่) - Management command
├── templates/
│   ├── aicashier/
│   │   ├── home.html                    (แก้ไข) - เพิ่มปุ่มเรียกพนักงาน
│   │   ├── order_management.html        (แก้ไข) - เพิ่มปุ่มยกเลิก
│   │   └── overviews/
│   │       └── overviews.html           (แก้ไข) - เพิ่มข้อมูล Analytics
├── urls.py                              (แก้ไข) - เพิ่ม API routes
├── views.py                             (แก้ไข) - Update OverviewsView
└── ...
```

---

## 🔧 การตั้งค่า

### 1. แจ้งเตือน Email
ตรวจสอบการตั้งค่า `settings.py`:
```python
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'  # หรือ email provider อื่น
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your-email@gmail.com'
EMAIL_HOST_PASSWORD = 'your-password'
DEFAULT_FROM_EMAIL = 'your-email@gmail.com'
```

### 2. Threshold สินค้าใกล้หมด
แก้ไขใน `services.py`:
```python
class InventoryService:
    LOW_STOCK_THRESHOLD = 10  # ← จำนวนชิ้น
```

### 3. Cron Job สำหรับตรวจสอบสินค้า
เพิ่มเข้า crontab (Linux/Mac):
```bash
# ตรวจสอบทุกชั่วโมง
0 * * * * cd /path/to/project && python manage.py check_low_stock

# หรือตรวจสอบทุกวัน เวลา 08:00
0 8 * * * cd /path/to/project && python manage.py check_low_stock
```

---

## 📊 Database Queries

ไม่มีการแก้ไขฐานข้อมูลเลย ✅  
ทั้งหมดใช้ Django ORM เพื่อดึงข้อมูลจาก models ที่มีอยู่

---

## 🔒 ข้อจำกัดและสิทธิ์การเข้าถึง

| ฟีเจอร์ | ลูกค้า | Cashier | Order Manager | Admin | SuperUser |
|--------|--------|---------|---------------|-------|-----------|
| เรียกพนักงาน | ✅ | ❌ | ❌ | ❌ | ❌ |
| ยกเลิกออเดอร์ (ตัวเอง) | ✅ | ❌ | ✅ | ✅ | ✅ |
| ยกเลิกออเดอร์ (ทั้งหมด) | ❌ | ❌ | ✅ | ✅ | ✅ |
| ดู Analytics | ❌ | ❌ | ✅ | ✅ | ✅ |
| ตรวจสอบสินค้าราคาถูก | ❌ | ❌ | ❌ | ❌ | ✅ |

---

## 🧪 การทดสอบ

### 1. ทดสอบปุ่มเรียกพนักงาน
```javascript
// ใน console browser
fetch('/api/staff/call/', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-CSRFToken': document.querySelector('[name=csrfmiddlewaretoken]').value
  },
  body: JSON.stringify({reason: 'ทดสอบ'})
})
.then(r => r.json())
.then(d => console.log(d))
```

### 2. ทดสอบยกเลิกออเดอร์
```javascript
fetch('/api/orders/cancel/', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-CSRFToken': document.querySelector('[name=csrfmiddlewaretoken]').value
  },
  body: JSON.stringify({order_id: 123, reason: 'ทดสอบ'})
})
.then(r => r.json())
.then(d => console.log(d))
```

### 3. ทดสอบแจ้งเตือนสินค้า
```bash
python manage.py check_low_stock
```

### 4. ทดสอบ Analytics API
```javascript
// AOV
fetch('/api/analytics/aov/?days=30')
.then(r => r.json())
.then(d => console.log(d))

// Cancellation Rate
fetch('/api/analytics/cancellation-rate/?days=30')
.then(r => r.json())
.then(d => console.log(d))

// Top Queries
fetch('/api/analytics/top-queries/?limit=5')
.then(r => r.json())
.then(d => console.log(d))
```

---

## 📞 Support & Troubleshooting

### ❌ ปุ่มเรียกพนักงานไม่ทำงาน
- ตรวจสอบการ login
- ตรวจสอบ CSRF token
- ตรวจสอบ browser console สำหรับ error

### ❌ Email ไม่ส่ง
- ตรวจสอบ `settings.py` - EMAIL configuration
- ตรวจสอบความสามารถของ email provider
- ตรวจสอบ logs: `python manage.py shell`

### ❌ Data Analytics ยังไม่ปรากฏ
- ต้องมี orders ที่ completed แล้ว
- ต้องเป็น staff user เท่านั้น
- รีเฟรชหน้า

---

## 🚀 อัปเดตในอนาคต

- [ ] บันทึกประวัติการเรียกพนักงาน (Notification History)
- [ ] Real-time notification สำหรับพนักงาน
- [ ] Machine Learning สำหรับคำถาม (NLP)
- [ ] Dashboard แบบ Real-time
- [ ] Mobile app สำหรับพนักงาน

---

## ✅ Checklist การติดตั้ง

- [ ] เพิ่ม `services.py`
- [ ] เพิ่ม `api_views.py`
- [ ] เพิ่ม `management/commands/check_low_stock.py`
- [ ] แก้ไข `urls.py`
- [ ] แก้ไข `views.py` (OverviewsView)
- [ ] แก้ไข `home.html`
- [ ] แก้ไข `order_management.html`
- [ ] แก้ไข `overviews.html`
- [ ] ตัั้งค่า Email (settings.py)
- [ ] ทดสอบทั้งหมด

---

**Version:** 2.0  
**Last Updated:** Feb 2026  
**Author:** AI Cashier Development Team
